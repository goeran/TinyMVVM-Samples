<UserControl x:Class="RichRememberTheMilk.WPF.Views.TaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Views="clr-namespace:RichRememberTheMilk.WPF.Views" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="TaskDataTemplate">
            <Views:TaskView/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ListBox HorizontalAlignment="Stretch" 
                 Margin="8,67,8,0" 
                 VerticalAlignment="Stretch" 
                 ItemTemplate="{DynamicResource TaskDataTemplate}" 
                 ItemsSource="{Binding Tasks}" Background="{x:Null}"/>
        <Button Content="Complete" HorizontalAlignment="Left" Height="24" Margin="8,8,0,0" VerticalAlignment="Top" Width="64"/>
        <Button Content="Postpone" HorizontalAlignment="Left" Height="24" Margin="76,8,0,0" VerticalAlignment="Top" Width="64"/>
        <ComboBox Height="24" Margin="144,8,8,0" VerticalAlignment="Top"/>
        <TextBox Height="23" Margin="8,40,8,0" TextWrapping="Wrap" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
